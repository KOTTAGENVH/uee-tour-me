import 'package:flutter/material.dart';

class CreditCardPay extends StatelessWidget {
  const CreditCardPay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text('pay here'));
  }
}
